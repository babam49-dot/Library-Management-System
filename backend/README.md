# Backend (Express + MySQL)

1. Copy `.env.sample` to `.env` and fill your DB credentials.
2. Install dependencies: `npm install` (run inside `backend` folder)
3. Run server: `npm run start` or `npm run dev`
